// src/components/VeteranProfile.tsx
import React, { useState, useEffect } from 'react';
import { motion } from 'framer-motion';
import { useWebSocket } from '@/hooks/useWebSocket';

interface VeteranData {
  id: string;
  name: string;
  ssn: string;
  fileNumber: string;
  branch: string;
  servicePeriod: string;
  dischargeType: string;
  disabilityRating: number;
  vadirAccuracy: number;
}

export const VeteranProfile: React.FC = () => {
  const [veteran, setVeteran] = useState<VeteranData | null>(null);
  const { data: metrics } = useWebSocket('/ws/metrics');

  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      className="bg-secondary rounded-2xl border border-border shadow-glow"
    >
      {/* Profile implementation */}
    </motion.div>
  );
};